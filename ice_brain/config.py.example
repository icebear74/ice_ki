# ============================================================================
# ICE BRAIN - Konfiguration
# ============================================================================
# Kopiere diese Datei als config.py und passe die Werte an:
#   cp config.py.example config.py

# Modell-Konfiguration
MODELS = {
    'router': {
        'path': '/models/qwen2.5-3b-instruct-q4_k_m.gguf',
        'gpu': 1,            # GPU-Index: 0=P100, 1=P4
        'n_ctx': 4096,
        'n_gpu_layers': -1,  # -1 = alle Layer auf GPU
    },
    'main': {
        'path': '/models/deepseek-r1-14b-q4_k_m.gguf',
        'gpu': 0,            # GPU-Index: 0=P100
        'n_ctx': 8192,
        'n_gpu_layers': -1,
    },
}

# MySQL-Konfiguration
MYSQL = {
    'host': 'localhost',
    'port': 3306,
    'user': 'ice_brain',
    'password': 'CHANGE_ME',
    'database': 'ice_brain',
    'pool_size': 5,
}

# Server-Konfiguration
HOST = '0.0.0.0'
PORT = 8000

# Embedding-Modell (für spätere Vektorsuche — Phase 2+)
EMBEDDING_MODEL = {
    'path': '/models/nomic-embed-text-v1.5.Q4_K_M.gguf',
    'gpu': 1,  # P4
}
